/**
 * Public API for the "reports" feature.
 * Import this feature ONLY through this barrel, e.g.:
 *   import { Something } from "@/features/reports";
 * Never import internal paths (@/features/reports/components/...) from outside.
 */
export {};
