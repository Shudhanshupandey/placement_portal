/**
 * Public API for the "resume" feature.
 * Import this feature ONLY through this barrel, e.g.:
 *   import { Something } from "@/features/resume";
 * Never import internal paths (@/features/resume/components/...) from outside.
 */
export {};
