/**
 * Public API for the "jobs" feature.
 * Import this feature ONLY through this barrel, e.g.:
 *   import { Something } from "@/features/jobs";
 * Never import internal paths (@/features/jobs/components/...) from outside.
 */
export {};
