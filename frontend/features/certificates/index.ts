/**
 * Public API for the "certificates" feature.
 * Import this feature ONLY through this barrel, e.g.:
 *   import { Something } from "@/features/certificates";
 * Never import internal paths (@/features/certificates/components/...) from outside.
 */
export {};
