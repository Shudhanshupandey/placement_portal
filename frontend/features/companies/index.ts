/**
 * Public API for the "companies" feature.
 * Import this feature ONLY through this barrel, e.g.:
 *   import { Something } from "@/features/companies";
 * Never import internal paths (@/features/companies/components/...) from outside.
 */
export {};
