/**
 * Public API for the "analytics" feature.
 * Import this feature ONLY through this barrel, e.g.:
 *   import { Something } from "@/features/analytics";
 * Never import internal paths (@/features/analytics/components/...) from outside.
 */
export {};
