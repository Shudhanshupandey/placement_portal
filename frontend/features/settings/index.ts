/**
 * Public API for the "settings" feature.
 * Import this feature ONLY through this barrel, e.g.:
 *   import { Something } from "@/features/settings";
 * Never import internal paths (@/features/settings/components/...) from outside.
 */
export {};
